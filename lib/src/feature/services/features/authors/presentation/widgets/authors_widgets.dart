import 'package:flutter/material.dart';

class AuthorsWidgets extends StatelessWidget {
  const AuthorsWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}